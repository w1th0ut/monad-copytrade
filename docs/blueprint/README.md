# nolosstrade

Workspace root for the hackathon build.

## Structure

- `fe/` - Next.js frontend app surface
- `be/` - backend / keeper placeholder
- `sc/` - smart contract placeholder
- `overview.txt`, `techarc.txt`, `flow.txt`, `be.txt` - blueprint docs

## Current milestone

Frontend app workspace first:

- trading interface inspired by modern perp DEX layouts
- TradingView chart in the core workspace
- market-only trade ticket
- leaderboard and vault pages derived from the blueprint
- trade app route lives at `/app`
- landing page route lives at `/`
